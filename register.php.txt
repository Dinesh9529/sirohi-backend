<?php
require_once 'utils.php';

$data = json_decode(file_get_contents('php://input'), true);
$phone = $data['phone'] ?? '';
$type = $data['type'] ?? '';

if (!verifyOTP($phone, $data['otp'] ?? '')) {
  respond(false, "OTP not verified");
}

// ?? Save user data
saveUser($data);
respond(true, "Registration successful");
